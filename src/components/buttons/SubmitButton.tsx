import Button from '@mui/material/Button';

export default function SubmitButton() {
  return (
    <Button
      style={{ marginRight: '10px' }}
      type="submit"
      variant="contained"
      color="success"
    >
      Submit
    </Button>
  );
}
